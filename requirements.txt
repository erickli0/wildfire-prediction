# GEE ingestion (ingest_landsat.py, ingest_dem.py --mode gee, ingest_weather.py,
# build_feature_table.py --mode gee). Needs an Earth Engine-enabled Google
# Cloud project passed via --project (ee.Initialize() with no project id
# fails on current earthengine-api). geemap>=0.38 is a hard floor here --
# it renamed geojson_to_ee -> vector_to_ee, and the scripts use the new name.
earthengine-api>=1.7.0
geemap>=0.38.0

# Raster/vector I/O and feature table building (ingest_dem.py --mode local,
# build_feature_table.py)
rasterio>=1.3.9
geopandas>=0.14.0
numpy>=1.26.0
pandas>=2.1.0

# Model training (train_model.py)
scikit-learn>=1.4.0
joblib>=1.3.0

# render_risk_map.py runs inside QGIS's own bundled Python environment
# (`qgis.core` comes from the QGIS install, not from pip) -- nothing to
# add here for that step.
